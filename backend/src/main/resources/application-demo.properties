# ── Demo profile ──────────────────────────────────────────────────────────────
# Drops and recreates all tables on every startup → always fresh demo data.
spring.jpa.hibernate.ddl-auto=create
# SQL init not needed; DataInitializer handles all demo data via Java.
spring.sql.init.mode=never
